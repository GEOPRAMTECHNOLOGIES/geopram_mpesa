{
  "name": "geopram-mpesa-vercel",
  "version": "1.0.0",
  "description": "GEOPRAM Services M-Pesa STK Push — Vercel serverless deployment",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "axios": "^1.6.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^7.5.0"
  }
}
"dependencies": {
  "dotenv": "^16.4.5"
}
